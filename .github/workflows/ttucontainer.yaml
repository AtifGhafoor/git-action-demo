name: container-actions
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/atifghafoor/teradata-ttu
    steps:
      - name: hello-world
        run: echo "Hello World. This is my first workflow run"
      - name: bteq-version
        run: which bteq
