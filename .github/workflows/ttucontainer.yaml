name: container-actions
on: push
permissions: 
  contents: read 
  packages: read
jobs:
  container-job:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/atifghafoor/teradata-ttu:20
    steps:
      - name: hello-world
        run: echo "Hello World. This is my first workflow run"
      - name: bteq-version
        run: which bteq
